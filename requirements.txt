# Versions used for the results reported in the paper.
# torch is listed without the CUDA suffix; install the build that matches your
# platform from https://pytorch.org (we used 2.6.0+cu126 on a GTX 1650).
torch==2.6.0
numpy==2.4.3
scikit-learn==1.8.0
matplotlib==3.8.4
pandas==3.0.1
scipy==1.17.1
